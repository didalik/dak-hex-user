{
  "name": "dak-hex-user",
  "description": "Stellar HEX: svc USER role support",
  "scripts": {
    "auth": "[ $USER = hexa ] || { echo 'NOT AUTHORIZED'; exit 1; }",
    "prod": "npm run auth && { cd prod/$npm_config_dir; ./${npm_config_run}.sh; }",
    "qa": "cd qa; ./loop.sh",
    "test": "cd test; make -k all"
  },
  "author": "Дід Alik",
  "license": "Apache 2",
  "private": true,
  "dependencies": {
    "@stellar/stellar-sdk": "^11.2.0"
  }
}
