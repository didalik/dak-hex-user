{
  "name": "dak-hex-user",
  "description": "Stellar HEX: the codebase for HEX actors",
  "scripts": {
    "auth_dev": "[ $HOME = /Users/alec -o $HOME = /home/alik ] || { echo 'NOT AUTHORIZED'; exit 1; }",
    "auth_prod": "[ $USER = hexa ] || { echo 'NOT AUTHORIZED'; exit 1; }",
    "dev": "npm run auth_dev && { cd dev; ./$npm_config_run $npm_config_dir; }",
    "prod": "npm run auth_prod && { cd prod/$npm_config_dir; ./${npm_config_run}; }",
    "qa": "cd qa; ./integration_tests",
    "test": "cd test; make -k all"
  },
  "author": "Дід Alik",
  "license": "Apache 2",
  "private": true,
  "dependencies": {
    "@stellar/stellar-sdk": "^11.2.0"
  }
}
